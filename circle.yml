---
  machine:
    node:
      version: "4.2.6"
  test:
    override:
      - npm run lint
      - npm run test-circle
      - npm run release
  deployment:
    production:
      branch: "master"
      commands:
        - npm publish
